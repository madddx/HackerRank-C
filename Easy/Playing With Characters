#include <stdio.h>

#define MAX_LEN 100

int main() {
    char c;
    char s[MAX_LEN];
    char sen[MAX_LEN];

    scanf("%c", &c);
    scanf("%s", s);
    scanf(" %[^\n]s", sen);


    printf("%c\n%s\n%s", c, s, sen);

    return 0;
}
